/** Cooldown between guard prompts in the singleplayer variant. */
export const COOLDOWN_MS = 2000;

/** Broadcast channel the admin uses to push a new duration to idle players. */
export const SETTINGS_CHANNEL = 'single:settings';
